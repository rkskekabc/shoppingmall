<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="productoption">

	<select id="getList" resultType="membervo">
		<![CDATA[
		select no, info, price, stock
		  from category
		]]>
	</select>
	
	<insert id="insert" parameterType="productoptionvo">
		<![CDATA[
			insert into product_option(info, price, stock) values(#{info }, #{price }, #{stock })
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>

	<select id="getByNo" resultType="membervo" parameterType='long'>
		<![CDATA[
		select no, name, parent_no
		  from category
		 where no = #{value }
		]]>
	</select>
	
	<update id="update" parameterType="productoptionvo">
		<![CDATA[
		update product_option
			set info=#{info }, price=#{price }, stock=#{stock } 
			where no=#{no }
		]]>
	</update>
	
	<delete id="delete" parameterType="long">
		<![CDATA[
		delete from product_option where no = #{value }
		]]>
	</delete>
</mapper>
